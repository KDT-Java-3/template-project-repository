package com.example.demo.lecture.refactorspringsection3;

import org.springframework.stereotype.Service;

/**
 * AFTER Placeholder.
 *
 * TODO 가이드:
 * - After Answer는 MapStruct Mapper + Service 메서드 분리를 통해 CRUD 로직을 가볍게 유지한다.
 * - validatePrice(), createProduct(), deactivate(), updateProduct(), toResponse() 등을 분리해 SRP를 지키자.
 * - 도메인 메서드/정책 클래스를 도입해 비즈니스 규칙을 명시적으로 표현해보자.
 */
@Service
public class RefactorSpringSection3ProductServiceAfter {
    // 실습 시 구현
}
